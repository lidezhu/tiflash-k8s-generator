apiVersion: pingcap.com/v1alpha1
kind: NetworkChaos
metadata:
  name: corrupt-tiflash
  namespace: {chaos_namespace}
spec:
  action: corrupt
  mode: one
  selector:
    namespaces:
      - {namespace}
    labelSelectors:
      "app": "tiflash"
  corrupt:
    corrupt: "40"
    correlation: "25"
  duration: "10s"
  scheduler:
    cron: "@every 15s"
