apiVersion: pingcap.com/v1alpha1
kind: NetworkChaos
metadata:
  name: loss-tiflash
  namespace: {chaos_namespace}
spec:
  action: loss
  mode: one
  selector:
    namespaces:
      - {namespace}
    labelSelectors:
      "app": "tiflash"
  loss:
    loss: "25"
    correlation: "25"
  duration: "10s"
  scheduler:
    cron: "@every 15s"
